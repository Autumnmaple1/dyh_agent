# 大运河人物智能体｜前端设计与开发需求书

## 1. 项目目标

制作一个 Vite + React 前端，服务于“大运河人物智能体”。用户先在真实地图上认识运河与三位人物，再选择其中一人进入专属对话。现有后端为 Python + FastAPI，现有文旅推荐、故事模式、对话历史与 API 行为不得重写。

首版人物：苏轼、陈瑄、张伯行。

核心体验不是普通地图导航，也不是卡片式后台，而是“可交互的当代水墨运河长卷”：真实地理位置负责可信度，水墨视觉负责文化氛围，界面层负责清晰选择人物。

## 2. 必须遵守的产品流程

1. 首次进入显示地图总览，不直接进入聊天。
2. 地图同时显示大运河线路、三个人物锚点及其真实地点。
3. 单击人物头像或人物名称，只改变当前选中态，并展开唯一一张人物详情面板。
4. 用户再次点击“与某某同行”后才进入聊天页。
5. 聊天页只允许与已选中的这一个人物对话；不能在聊天页内直接切换人物。
6. 若要更换人物，必须“返回运河地图”，重新选择。
7. 聊天页保留两个模式：
   - 游历：人物以符合其时代、身份和心境的口吻进行当地大运河文旅推荐。
   - 入境：进入预设场景，由 AI 推进剧情，让用户通过选择和对话参与故事。
8. 保留现有会话历史管理、清空会话、发送消息、加载态、错误态等功能，不改后端协议。

## 3. 真实地点与数据原则

### 3.1 地图语义

必须区分：

- `characterSite`：人物确有直接关联的现实文化地点。
- `canalSite`：与该城市大运河文旅相关的现实地点。

两者不能混为一谈。人物不应被随意钉在一座与其没有直接史实关系的桥或景点上。地图可用不同图形区分：人物点使用头像标记，运河地点使用小型水滴或桥梁符号。

### 3.2 首版地点

#### 苏轼｜徐州

- 人物地点：黄楼／黄楼公园
- 真实地址：江苏省徐州市鼓楼区黄河南路黄楼公园，庆云桥附近
- 高德 GCJ-02 坐标：`[117.186410, 34.271570]`
- 关联依据：黄楼由苏轼在徐州抗洪后修建，是其徐州治水与城市记忆的直接文化地点。
- 运河文旅关联：徐州故黄河水系、百步洪故址、徐州古城水系。

#### 陈瑄｜淮安

- 人物地点：陈潘二公祠
- 真实地址：江苏省淮安市清江浦区闸口街道城东社区里运河畔轮埠路，吴公祠旁
- 关联依据：该祠直接纪念陈瑄与潘季驯，展陈治水、漕运与大运河历史。
- 运河地点：里运河文化长廊、清江大闸、中洲岛／清江浦楼一带。
- 实现要求：使用高德地理编码服务对上述完整地址进行一次精确解析，人工在高德地图核对 POI 后，把返回的 GCJ-02 坐标固化进数据文件。不得使用“淮安市中心”或“清江浦区中心”代替。

#### 张伯行｜苏州

- 人物地点：江苏巡抚衙门旧址
- 真实地址：江苏省苏州市姑苏区书院巷20号（苏州卫校内）
- 关联依据：张伯行于清康熙后期任江苏巡抚并在苏州任职。此点作为其任职机构的现实遗址锚点，不等同于张伯行故居，也不表示现存建筑全部为其任内原物。
- 运河地点：宝带桥
- 宝带桥真实地址：江苏省苏州市吴中区宝南路北端
- 宝带桥高德 GCJ-02 坐标：`[120.654001, 31.255173]`
- 说明：宝带桥是大运河苏州段的真实文旅点，但不应标注为“张伯行遗址”。
- 实现要求：同样通过高德地理编码和人工 POI 核对，固化江苏巡抚衙门旧址坐标。

### 3.3 坐标与地图实现约束

- 中国大陆地图统一采用高德地图 JS API 2.0 与 GCJ-02 坐标。
- 禁止混用 WGS84、百度 BD-09 和 GCJ-02，否则点位会发生偏移。
- 地点坐标最终必须是静态数据，不应每次打开页面都重新地理编码。
- 地图底层必须是真实可缩放、拖动的地图；水墨纹理只是覆盖层，不能用一张静态水墨画冒充真实地图。
- 使用自定义 Marker 表现人物头像；使用 Polyline 或 GeoJSON 表现运河线路。
- 运河线路必须来自真实轨迹数据或经过核验的简化 GeoJSON。可以为了视觉清晰减少节点，但不得改变城市经过关系。
- 初始视野自动包含三个人物点和运河主线；选择人物后平滑移动到该人物地点，不进行夸张飞行动画。

## 4. 页面结构

### 4.1 地图总览页 `/`

采用“地图优先”的 C 方向：

- 顶部为紧凑单行导航，仅保留品牌“运河人物志”和必要操作。
- 主体几乎全部交给真实地图。
- 左上角显示短标题“沿水路，与谁同行”，不要添加大段介绍文案。
- 三个人物头像标记直接落在各自真实人物地点。
- 地图上另绘制真实大运河线路和必要的运河地点。
- 默认没有人物详情大卡；默认可弱选中苏轼，或保持无选中状态。
- 选择人物后，只出现一张紧凑详情面板，内容为头像、姓名、朝代、城市、真实地点、人物一句话简介和主按钮。
- 主按钮文案随人物变化：`与苏轼同行`、`与陈瑄同行`、`与张伯行同行`。
- 地点信息必须可读，例如“人物地点 · 黄楼公园”，不能只写“徐州”。
- 可提供“定位到大运河全景”的小型图标按钮，但不增加无关工具栏。

### 4.2 单人物聊天页 `/chat/:characterId`

- 顶部清楚显示当前人物头像、姓名、城市与返回地图按钮。
- 不显示其他两个人物的头像或切换入口。
- 保留“游历／入境”模式切换；采用小尺寸分段控件，不能做成大胶囊。
- 对话区保持舒适阅读宽度；历史消息按角色区分，但减少厚重聊天气泡。
- 人物回答可使用米纸色浅底和细边框，用户消息使用克制的深玉色。
- 输入区固定在内容底部，不遮挡最后一条消息。
- 模式切换、返回地图或清空对话时，若会导致上下文丢失，需要明确提示。

## 5. 视觉方向

视觉关键词：`当代水墨`、`冷玉色`、`米纸留白`、`克制的 Liquid Glass`、`真实地图`。

- 主色：深墨绿 `#173f3a`，交互玉色 `#1f6a61`。
- 背景：冷米纸色 `#f3f5f1`，不要黄褐色仿古滤镜。
- 辅助色：雾灰绿 `#9eaaa4`，边框使用低对比半透明墨色。
- 水墨只用于地图纹理、远山、河雾和局部过渡，不覆盖地图标签与人物信息。
- Liquid Glass 只用于顶部栏和当前人物详情面板，禁止所有卡片都玻璃化。
- 圆角控制在 `4–10px`；现有过大圆角缩小到原来的约三分之一。
- 禁止大胶囊、巨型圆角容器、密集阴影、霓虹、紫色、金色古风、旅游宣传页式大标题。
- 中文标题可使用有文化气质的宋体／衬线体，功能文字使用高可读无衬线体。
- 保持大量留白和一条明确的视觉主线：真实运河。
- 除人物名、地点、朝代、必要按钮外，尽量去除装饰性文字。

## 6. 动效

- 人物选中：头像边缘出现一次轻微玉色水波扩散，时长约 240–320ms。
- 地图定位：使用平滑 `panTo`／`setZoomAndCenter`，不旋转、不弹跳。
- 详情面板：短距离淡入和位移，不能使用夸张弹簧动画。
- 进入聊天：地图雾化淡出，聊天页淡入；总时长不超过 400ms。
- 支持 `prefers-reduced-motion`，关闭水波和位移动效。

## 7. 响应式

### 桌面端（≥1024px）

- 地图占主体；详情面板停靠左下或右下，不遮挡选中标记。
- 顶栏高度紧凑，首屏无需滚动。

### 平板端（768–1023px）

- 地图仍为主体；详情面板缩窄，并自动避让标记。
- 保证三个头像标记可独立点击。

### 手机端（<768px）

- 地图占上部约 60–68vh。
- 选中人物后，底部出现可收起的详情抽屉；抽屉圆角仍保持克制。
- 地图控件与底部抽屉之间至少保留 16px 安全距离。
- 聊天页输入框适配软键盘和安全区，不能被虚拟键盘遮挡。

## 8. 组件与状态建议

建议组件：

- `CanalMap`
- `CharacterMarker`
- `CanalSiteMarker`
- `CharacterInspector`
- `MapTopBar`
- `CharacterChatHeader`
- `ModeSwitcher`
- `MessageList`
- `ChatComposer`
- `ConfirmDialog`

地图页状态至少包括：`idle`、`mapLoading`、`mapError`、`characterSelected`、`navigatingToChat`。

聊天页状态至少包括：`loadingHistory`、`ready`、`sending`、`streaming`（若 API 支持）、`requestError`、`clearingSession`。

## 9. 推荐数据结构

```ts
type CharacterMapItem = {
  id: "su-shi" | "chen-xuan" | "zhang-boxing";
  name: string;
  dynasty: string;
  city: string;
  portrait: string;
  shortIntro: string;
  characterSite: {
    name: string;
    address: string;
    position: [number, number]; // GCJ-02 [lng, lat]
    relationNote: string;
  };
  canalSites: Array<{
    name: string;
    address: string;
    position: [number, number];
    relationNote: string;
  }>;
};
```

## 10. 技术边界

- 技术栈：Vite + React；可使用现有 `@phosphor-icons/react`。
- 地图：高德地图 JS API 2.0，Key 和安全密钥必须从环境变量读取，不写死在仓库中。
- 不重写 FastAPI，不改变现有 API 请求和返回结构。
- 不引入重量级 UI 组件库，不做通用后台模板。
- 人物图片使用现有资源；如需水墨纹理，只作为压缩后的 WebP/AVIF 装饰层。
- 路由刷新后仍能恢复所选人物；无效人物 ID 应返回地图并显示简短提示。
- 首屏图片和地图脚本需要加载占位；地图加载失败时展示可重试的降级界面，不能白屏。

## 11. 可访问性与性能

- 所有人物 Marker 同时支持鼠标、触控和键盘选择，并具有可读 `aria-label`。
- 头像和文字都可触发选择，点击区域至少 44×44px。
- 焦点样式清楚，不只依赖颜色表达选中。
- 正文和按钮对比度达到 WCAG AA。
- 地图装饰层 `pointer-events: none`，不能阻碍地图操作。
- 人物图片指定尺寸，避免布局抖动；首屏以外资源懒加载。
- 目标：桌面和主流手机无横向滚动；地图交互保持流畅。

## 12. 验收标准

1. 打开网站首先看到真实地图，而不是聊天窗口或静态插画。
2. 地图可以缩放、拖动，人物点位落在真实文化地点。
3. 大运河线路与人物点使用同一坐标系，无明显偏移。
4. 任意时刻地图上只有一个人物处于选中态，只有一张人物详情面板。
5. 点击“与某某同行”后进入该人物专属聊天，无法在聊天页偷换人物。
6. 返回地图后可以重新选择人物。
7. 游历、入境、发送、历史记录、清空和错误反馈均可使用。
8. 1440×900、1024×768、390×844 三种尺寸均无溢出、遮挡或无法点击的问题。
9. 视觉符合冷玉色当代水墨，圆角小且克制，没有模板化大卡片和多余文案。
10. 所有真实地点在高德地图中人工复核，地址、坐标、人物关联说明分别正确。

## 13. 交付要求

请交付可运行的 Vite + React 源码、依赖说明、环境变量示例、地图数据文件、运河 GeoJSON、桌面与手机截图。先完成地图总览页和到聊天页的完整选择流程，不扩展后台管理、登录、注册或其他非必要功能。
